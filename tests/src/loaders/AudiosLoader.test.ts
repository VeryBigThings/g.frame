import AudiosLoader from '../../../src/loaders/AudiosLoader';

const audiosLoader = new AudiosLoader();

describe('Audios Loader testing - TODO', () => {
    test('Checking#1', () => {
        expect(1).toBe(1);
    });
    test('Checking#2', () => {
        expect(2).toBe(2);
    });
    test('Checking#3', () => {
        expect(3).toBe(3);
    });
});

