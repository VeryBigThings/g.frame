import VideosLoader from '../../../src/loaders/VideosLoader';

const videosLoader = new VideosLoader();

describe('Videos Loader testing - TODO', () => {
    test('Checking#1', () => {
        expect(1).toBe(1);
    });
    test('Checking#2', () => {
        expect(2).toBe(2);
    });
    test('Checking#3', () => {
        expect(3).toBe(3);
    });
});

